DATABASE_URL="postgresql://postgres:postgres@localhost:5432/legal_chatbot"

ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL=""

EMBEDDING_PROVIDER="none"
OPENAI_API_KEY=""
OPENAI_EMBEDDING_MODEL="text-embedding-3-small"
VOYAGE_API_KEY=""
VOYAGE_EMBEDDING_MODEL=""

NEXT_PUBLIC_APP_NAME="정비사업 법령 AI"
MAX_UPLOAD_SIZE_MB="20"

# 세션 쿠키 서명 키(HMAC). 반드시 무작위 값으로 설정한다 (예: openssl rand -hex 32).
# 비워두면 개발 전용 기본값이 사용되며, 이 상태로는 세션 쿠키 위변조를 막을 수 없다.
SESSION_SECRET=""
